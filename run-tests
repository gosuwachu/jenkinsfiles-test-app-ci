#!/bin/bash
set -e

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
VENV="$SCRIPT_DIR/.venv"

if [ ! -d "$VENV" ]; then
    python3 -m venv "$VENV"
fi

"$VENV/bin/pip" install --quiet -r "$SCRIPT_DIR/requirements-dev.txt"

PYTHONPATH="$SCRIPT_DIR/src" exec "$VENV/bin/pytest" "$@"
